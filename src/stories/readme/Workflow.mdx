import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Workflow" />

# Создание истории компонента

###### Пошаговая инструкция может и будет меняться со временем. <br/> Если инструкция не соответствует версии Storybook прошу сразу приводить ее к актуальной версии.

- Создаем раздел в **_src/stories/components_** по названию описываемого компонента. <br/>
  Например создадим историю для компонента UiBlock. <br/>
  Нам необходимо перейти в раздел **_src/stories/components_** и создать там раздел **_UiBlock_** <br/>
  Должна получится такая структура: <br/>
  📁 **src**  
  ┣ 📁 **stories**  
  ┃ ┗ 📁 **components**  
  ┃ ┗ 📁 **UiBlock**

- Создаем файл в **_src/stories/components/UiBlock_** с окончанием на **_\*.stories.ts_** <br/>
  В нашем случа это **_UiBlock.stories.ts_**. <br/>
  Должна получится такая структура: <br/>
  📁 **src**  
  ┣ 📁 **stories**  
  ┃ ┗ 📁 **components**  
  ┃ ┗ 📁 **UiBlock**  
  ┃ ┗ 📄 **UiBlock.stories.js**

- Начинаем с импортов

```ts
// Импортируем типы
import type { Meta, StoryObj } from '@storybook/vue3-vite';

// Импортируем сам компонент
import UiBlock from '@/components/UiBlock/UiBlock.vue';
```

- Создаем объект компонента и экспортируем его. <br/>
  Таким образом мы создаем мастер-шаблон для последующих интерпретаций

```ts
const meta = {
  // Название кнопки в меню Storybook
  title: 'Примеры/UiBlock',
  // Сам компонент
  component: UiBlock,
  // Авто-документация - https://storybook.js.org/docs/writing-docs/autodocs
  tags: ['autodocs'],
  // Настройки отображения и типов Props компонента
  argTypes: {},
  // Props компонента
  args: {
    title: 'Блок',
    rounded: false,
    bordered: false,
    cascadeColored: false,
    accordion: false,
  },
} satisfies Meta<typeof UiBlock>;

export default meta;
type Story = StoryObj<typeof meta>;
```

- Создаем следующие интерпретации если того требует компонент. <br/>
  Или если у компонента несколько разных состояний таким образом мы можем отобразить их все. <br/>
  В нашем случа мы отобразим еще 4 состояния:
  - Скругленный
  - Скругленный
  - Скругленный
  - Скругленный

```ts
// Скругленный
export const Rounded: Story = {
  args: {
    rounded: true,
  },
};
// Окантовка
export const Bordered: Story = {
  args: {
    bordered: true,
  },
};
// Меняет цвет от уровня вложенности в такие же компоненты
export const CascadeColored: Story = {
  args: {
    cascadeColored: true,
  },
};
// Складывается
export const Accordion: Story = {
  args: {
    accordion: true,
  },
};
```

#### На этом создание компонента закончено. Он будет автоматически размещен в разделе **"Примеры"** основного меню.
